apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ts-exporter
spec:
  rules:
    - host: ts-exporter-dev.k8s.moody
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ts-exporter
                port:
                  number: 5000